# Разбор плана Ярослава от 06.09.2026 — что берём, что откладываем и почему

> Источник — переписка на рабочем столе (`План.txt`, ~40 000 символов, четыре сообщения
> Ярослава от 21:13 до 21:39). Здесь она превращена в ПРОВЕРЯЕМЫЙ бэклог: у каждого
> пункта сказано, сделан он, делается или отложен — и по какой причине.
>
> ⚠️ **Документ заведён потому, что план в чате — это не план.** Сорок тысяч символов
> прозы нельзя ни выполнить, ни проверить: их можно только «прочитать и согласиться».
> Через месяц никто не скажет, какие пункты закрыты, а какие тихо пропущены.

> 🔴 **ЭТО ЖИВОЙ СПИСОК, А НЕ ОТЧЁТ О ЗАХОДЕ 06.09.** Статус правится ТЕМ ЖЕ заходом,
> что и код. Причина записана ценой прямого замечания Ярослава 10.09.2026: «почему я
> тебе всё это заново пишу? я сто лет назад уже все эти правки давал». Он был прав по
> факту — работа уезжала, а документ стоял. В нём, например, значилось «Ed25519-подпись
> обновлений — **не сделан**, нужно решение Ярослава», когда подпись уже была заведена и
> проверена. Документ, по которому сверяют состояние, соврал — и человек справедливо
> начал диктовать требования заново, потому что верить документу стало нельзя.
> Это тот же записанный урок, что «устаревший CLAUDE.md хуже, чем его отсутствие»,
> просто в другом файле.

## Главный вывод разбора

План состоит из ТРЁХ независимых глав, и смешивать их нельзя:

| глава | суть | масштаб |
|---|---|---|
| 1. Sync Fabric | журнал операций вместо снимков, курсор вместо `since`, типизированные слияния | **несколько релизов**, этапы 0–5 |
| 2. База данных | наблюдаемость, индексы ПОСЛЕ замера, ledger миграций | недели, требует нагрузочного стенда |
| 3. Безопасность | подпись обновлений, WORM-бэкап, hardening, зачистка утечек | часть — сейчас, часть — только на новом железе |

⚠️ **Половина пунктов невыполнима без решений и железа, которых у меня нет:** offline-ключ
подписи, отдельное WORM-хранилище, production-ПК с TPM, VPN для админского доступа. Их
нельзя «сделать наполовину» — недоделанная подпись обновлений опаснее её отсутствия,
потому что создаёт уверенность.

---

## Что сделано в этом заходе (06.09.2026)

| пункт плана | что сделано |
|---|---|
| P1 «GigaChat credential возвращается администратору» | ключ стал **write-only**: наружу уходит признак «настроен / не настроен», значение не читается никогда |
| P0/эксплуатация «проверка версии SQLite (WAL-reset гонка, 3.51.3)» | версия SQLite и состояние WAL видны в диагностике сервера; ниже исправленной — честное предупреждение |
| глава 1, этап 0 «спецификация до кода» | этот документ + матрица инвариантов ниже |

---

## Глава 1. Sync Fabric — что именно откладывается и почему

Ярослав сам расставил этапы и риски, и с ними я согласен без оговорок:

| этап | суть | риск по плану | статус |
|---|---|---|---|
| 0 | спецификация Sync Protocol v2 и матрица инвариантов | низкий | **делается** (см. ниже) |
| 1 | `base_revision` и структурированные конфликты для оценок | средний | не начат |
| 2 | `SyncProjectionRegistry` и contract-тесты прав/полей | средний | не начат |
| 3 | локальный durable `sync_ops` journal | **высокий, отдельный релиз** | не начат |
| 4 | `sync_changes` + opaque cursor + `resync_required` | **высокий, отдельный релиз** | не начат |
| 5 | SSE wake-up и privacy-safe diagnostics | низкий после v2 | не начат |

🔥 **Почему нельзя начать с этапа 3 или 4, хотя именно они чинят самое больное.**
Локальный журнал операций обязан создаваться В ОДНОЙ ТРАНЗАКЦИИ с бизнес-правкой — иначе
он «почти работает», а это худшее состояние для синхронизации. Ярослав пишет об этом
прямо: «ручное добавление из нескольких UI-путей неизбежно когда-нибудь пропустит
изменение». У нас записи в локальную базу идут из десктопа, офлайн-очереди веба и
слияния с сервера; свести их в один write boundary — это и есть отдельный релиз, а не
часть захода по багам.

### Матрица инвариантов (этап 0) — то, что обязано остаться истинным при любом протоколе

Формулировка Ярослава: «оценка либо применена один раз, либо явно в конфликте; никогда не
исчезла и не изменилась молча». Разворачиваю в проверяемые утверждения:

1. **Идемпотентность.** Повтор доставки одного `operation_id` не создаёт второй записи и
   не меняет результат. (Сегодня частично есть: `client_nonce` в мессенджере, `push`
   идемпотентен по содержимому.)
2. **Ни одной потерянной правки.** Падение процесса между локальной записью и отправкой
   не теряет операцию. (Сегодня НЕ выполняется: `_push_watermark` двигается по времени.)
3. **Конфликт заметен.** Расхождение значения оценки не разрешается молча в пользу
   последнего. (Сегодня выполняется наполовину: `_merge_grades` пишет в `sync_conflicts`,
   но **разбирать их нечем** — диалог удалён вместе с Qt, и `list_conflicts` не зовёт
   никто. Это записано в самом коде и остаётся честным долгом.)
4. **Причинность объяснима.** Про любую правку можно сказать, кто и на каком устройстве
   её сделал. (Сегодня частично: `Grade.device`.)
5. **Роль не расширяется синхронизацией.** Через `/sync/push` нельзя записать то, чего
   роль не может записать через веб. (Сегодня выполняется, держат тесты `test_sync.py`.)

⚠️ **Что из плана я СОЗНАТЕЛЬНО не рекомендую даже в будущем** — и Ярослав тут прав
дословно: CRDT для оценок («4» и «5» не сливаются), P2P/WebRTC, внешний replication-сервис
(Electric/PowerSync), WebSocket как основа надёжности.

---

## Глава 2. База данных — что делать и в каком порядке

Ключевая мысль плана, с которой надо начинать: **«индексировать всё» не оптимизация**.
У нас ОДИН writer, и каждый лишний индекс делает запись тяжелее.

| пункт | статус | почему так |
|---|---|---|
| read-only диагностика (версия SQLite, WAL, page_count, freelist) | **сделано** | дёшево, ничего не меняет, сразу отвечает на «а что там вообще происходит» |
| проверка версии SQLite против 3.51.3 (WAL-reset гонка) | **сделано** | эксплуатационный контроль, не авария |
| синтетический датасет + p50/p95 + `EXPLAIN QUERY PLAN` в CI | не начат | нужен отдельный стенд; без него составные индексы добавлять НЕЛЬЗЯ |
| составные индексы из таблицы плана | **не добавлены намеренно** | план сам требует «только после EXPLAIN». Добавить «на веру» — ровно то, от чего он предостерегает |
| Schema Ledger вместо списка `_ensure_*` | не начат | наш нынешний способ работает и покрыт регрессиями; менять его без нагрузки — риск без выгоды |
| «не держать writer во время сети» (принцип Convex) | частично есть | у нас уже вынесены в пул потоков Whisper, перевод, приманки; отдельного правила нет |

---

## Глава 3. Безопасность — построчно

### P0 (до переезда на production-ПК)

| пункт | статус | честная причина |
|---|---|---|
| Ed25519-подпись обновлений, публичный ключ в desktop/APK | ✅ **СДЕЛАНО 10.09.2026** | ключ заведён, открытая половина в `desktop_update.UPDATE_PUBLIC_KEYS`, закрытая — `~/.gradebook/release_signing_key.hex` вне репозитория. Подпись стала ШАГОМ выкладки с воротами `--verify` (нашлось, что `sign_release.py` не вызывался никем — парк остался бы без обновлений молча). ⚠️ Честная граница: ключ лежит файлом на машине выкладки, а не в offline-хранилище — настоящая offline-подпись остаётся отдельной работой |
| 3-2-1-1-0 backup с WORM/Object Lock | **не сделан** | нужно ВНЕШНЕЕ хранилище и учётная запись с правом писать, но не удалять. Это закупка, а не код |
| audit checkpoint вне production-хоста | **не сделан** | упирается в то же хранилище. Механизм снятия точки у нас уже есть (`verify_audit.py --checkpoint`) — некуда отправлять |
| backend не от root + systemd hardening | ✅ **НАПИСАНО 10.09.2026, ждёт машины** | `deploy/install.sh` заводит системного пользователя `gradebook`, переносит базу и вложения в `/var/lib/gradebook` (StateDirectory), делает каталог кода только-для-чтения и применяет весь набор: `NoNewPrivileges`, `ProtectSystem=strict`, `ProtectHome`, `PrivateTmp`, `PrivateDevices`, `ProtectKernelTunables`, `ProtectKernelModules`, `ProtectControlGroups`, пустой `CapabilityBoundingSet=`, `RestrictAddressFamilies=AF_UNIX AF_INET AF_INET6`, `SystemCallFilter=@system-service`. Держат 11 сторожей в `server/tests/test_install_script.py` (с обратным ходом). Staging — `tools/staging_unit_check.sh` (три ступени: разбор, ЗАПУСК под копией имени, `systemd-analyze security`). 🔥 Умолчание `APP_DIR` пришлось сменить с `/root/gb-deploy` на `/opt/gradebook`: `ProtectHome=yes` отрезает /root, и служба не нашла бы собственный `serve.py`. Ровно тот отказ, ради которого план и требовал не применять вслепую |
| секреты в `LoadCredentialEncrypted` | ✅ **ИНСТРУМЕНТ ГОТОВ 10.09.2026, на бою не применён** | `tools/migrate_secrets_to_credentials.sh`: шифрует `systemd-creds` (`--with-key=auto`, на TPM-хосте привязка к машине), **читает обратно и сверяет ДО очистки .env** (иначе первая же ошибка шифрования уничтожила бы единственную копию ключа от базы), кладёт резервную копию и требует унести её с машины. Список имён берётся из `secrets_source.SECRET_NAMES`, а не переписан второй копией. Установщик зовёт его сам и вписывает `LoadCredentialEncrypted=` в юнит. ⚠️ На ЖИВОМ VPS перенос не делался — там служба стоит с прежним юнитом |

### P1

| пункт | статус |
|---|---|
| GigaChat credential write-only | **сделано** |
| JWT из `localStorage` → BFF/HttpOnly cookie | не начат. Граница, выясненная 10.09.2026: **cookie годится только браузеру.** Сайт и API живут на одном origin (`esstu-gradebook.ru`), значит `SameSite=Strict` + CSRF-токен работают. Android — origin `https://localhost` против чужого API, то есть cookie межсайтовая и её режет блокировка сторонних; десктоп ходит на `127.0.0.1`. Поэтому сервер обязан принимать ОБА способа, а защита от CSRF — включаться только на cookie-пути |
| админам два passkey + обязательная user verification | не начат. ⚠️ **Прежняя оговорка «второй фактор у админа снят» ПРОВЕРЕНА 10.09.2026 и оказалась неверной**: он действует — `deps._require_second_factor_setup` не пускает админа без TOTP ни в одну ручку на бою. То есть пункт НЕ заблокирован и его можно начинать. Что нужно: поле «проверен ли пользователь» у `WebAuthnCredential` (сейчас его нет вовсе), требование минимум двух ключей у роли `admin`, коды восстановления офлайн, TOTP как запасной путь |
| authorization fuzzing matrix (роль × ручка × объект) | не начат, но это чистые тесты и нулевой риск для боя — лучший кандидат на следующий заход |
| LLM privacy boundary как ТИП, а не флаг | частично: сторож-свойство `test_vector_never_voices_names.py` уже есть; единого builder-а контекста нет |
| Argon2id / 15 символов для password-only | не начат; **требует решения**: смена минимума ломает вход существующим людям |

### P2

Закрепление GitHub Actions по SHA, artifact attestations, жёсткий security-gate по CVE,
SAST для JS, ежеквартальные restore drill и security scorecard — не начаты, риска для боя
не несут, делаются отдельным заходом.

---

## Что я предлагаю следующим заходом

1. **Authorization fuzzing matrix** — чистые тесты, нулевой риск, закрывает самый частый
   класс уязвимости API (BOLA). Лучшее соотношение пользы к риску во всём плане.
2. **systemd hardening в `deploy/install.sh`** + проверка `systemd-analyze security` —
   касается будущей машины ВСГУТУ, живой юнит не трогает.
3. **Стенд для замеров** (синтетический датасет + `EXPLAIN QUERY PLAN`) — без него глава 2
   не двигается вообще.

## Что требует решения Ярослава, а не работы

* где живёт **offline-ключ подписи** обновлений (без ответа пункт P0 не начать);
* какое **внешнее хранилище** для WORM-бэкапа и audit anchor (закупка);
* минимальная **длина пароля** для входа без второго фактора — 15 символов ломает вход
  существующим людям, это решение продукта, а не техники;
* **passkey у администратора** — прямо конфликтует с только что снятым вторым фактором.

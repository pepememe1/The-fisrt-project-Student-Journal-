<!--
Вынесено из CLAUDE.md 03.09.2026.

ПОЧЕМУ: CLAUDE.md весил ~120 000 токенов и грузился ЦЕЛИКОМ в каждую сессию И в каждого
субагента — три грани параллельно стоили 360 000 токенов до первой полезной строки.
Именно это давало отказы по лимиту 01-02.09.2026.

ЧТО СДЕЛАНО: раздел перенесён ДОСЛОВНО, ни одна строка не удалена. В CLAUDE.md на его
месте остался указатель с самым важным — тем, что должно быть известно всегда, не
открывая этот файл.

ЕСЛИ ПРАВИТЕ ЭТОТ ФАЙЛ: проверьте, не устарел ли указатель в CLAUDE.md. Их расхождение
держит `tests/test_claude_md_split.py`.
-->

## 15. Навыки, плагины и MCP (установлено 18.08.2026)

⚠️ **Здесь раньше было написано «установлены 47 маркетинг-скиллов Corey Haines».
Это оказалось неправдой** — в `~/.claude/skills/` лежал РОВНО ОДИН навык, `graphify`.
Классический случай: документация описывала намерение как факт, и по ней делались выводы.
Проверено 18.08.2026 прямым `ls`, а не по памяти.

**Что стоит на самом деле** (пользовательский уровень, доступно во всех сессиях):

**MCP.** ⚠️ Список ниже — намерение, а не факт: сверка 24.08.2026 показала, что
`claude mcp list` не отдавал НИ ОДНОГО из них. В тот день поставлен и проверен только
`playwright`; остальные шесть **не подключены**, и прежде чем на них рассчитывать —
сверься командой, а не этим абзацем.
`playwright` (Microsoft — живая проверка интерфейса, см. запрет dev-browser в шапке; ✅ стоит),
`context7` (Upstash — свежая дока библиотек), `notion`, `github`, `supabase`, `firecrawl`,
`telegram`. Последние пять — из ОФИЦИАЛЬНОГО каталога `anthropics/claude-plugins-official`,
а не с личных форков: у половины ссылок из первого списка автор был случайным человеком.
⚠️ Notion и Supabase требуют входа через `/mcp` в интерактивном окне; GitHub, Firecrawl и
Telegram — токенов. Без этого их инструменты не работают.

**Плагины:** `superpowers` (TDD, systematic-debugging, verification-before-completion — их
дисциплина совпадает с нашей: обратный ход у сторожа, доказательство до заявления),
`document-skills` (Word/Excel/PowerPoint/PDF), `example-skills` (в т.ч. `webapp-testing`),
`security-guidance` (хук-предупреждение при правке чувствительных файлов).

**Навыки — 139 штук**: `claude-seo` (33), `agent-skills` Эдди Османи (24),
`ai-design-components` (76), `ui-ux-pro-max`, `dataviz`, `graphify`.

⚠️ **Цена измерена, и её надо знать:** описания 139 навыков — это ~19 тысяч токенов в
КАЖДОЙ сессии. Терпимо на фоне того, что шапка этого файла стоила ~200 тысяч (потому её и
вынесли), но не бесплатно. Не нужен раздел навыков — `claude plugin disable <имя>`;
`claude plugin details <имя>` показывает прогноз расхода по конкретному плагину.

⚠️ **Плагин исполняется с ТЕМИ ЖЕ правами, что и Claude**, в репозитории, где лежит ключ
от боевого VPS и ПДн студентов. Ставить только из официального каталога Anthropic либо от
автора, которого можно назвать поимённо. Решение по текущему набору принял Ярослав.

Для маркетинговых задач (РФ-рынок EdTech, 44-ФЗ/223-ФЗ, 152-ФЗ, покупатель — бюджетное
учреждение, считать в рублях и в циклах бюджета вуза) отдельных навыков сейчас НЕТ —
это делается обычным разговором, а не вызовом скилла.
